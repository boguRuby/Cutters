<%= form_for(cutter) do |f| %>
  <% if cutter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cutter.errors.count, "error") %> prohibited this cutter from being saved:</h2>

      <ul>
      <% cutter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
